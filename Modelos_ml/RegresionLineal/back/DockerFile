FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

#Entrena y genera el artefacto ne el build
RUN python train.py

EXPOSE 8000

CMD ["uvicorn", "Modelos_ml.RegresionLineal.back.main:app", "--host", "0.0.0.0", "--port", "8000"]